apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../../../infra/packages/base
patches:
  - target:
      kind: Kustomization
      name: home-oci-packages-base
      namespace: flux-system
    patch: |-
      apiVersion: kustomize.toolkit.fluxcd.io/v1
      kind: Kustomization
      metadata:
        name: home-oci-packages-base
        namespace: flux-system
      spec:
        patches:
          - target:
              kind: HelmRelease
              name: sealed-secrets
              namespace: base
            patch: |-
              apiVersion: helm.toolkit.fluxcd.io/v2
              kind: HelmRelease
              metadata:
                name: sealed-secrets
                namespace: base
              spec:
                values:
                  ingress:
                    hostname: sealed-secrets.andrewlee.fun
