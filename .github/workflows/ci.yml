name: CI

on:
  push:
    tags:
      - v?[0-9]+.[0-9]+.[0-9]+*

  workflow_dispatch:
    inputs:
      tag:
        description: The existing tag to publish to FlakeHub
        type: string
        required: true

jobs:
  CI:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      pages: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          persist-credentials: false
          ref: ${{ (inputs.tag != null) && format('refs/tags/{0}', inputs.tag) || '' }}

      - name: Install Nix  
        uses: DeterminateSystems/determinate-nix-action@main

      - name: Nix Cache
        uses: DeterminateSystems/flakehub-cache-action@main

      - name: Flake Checker
        uses: DeterminateSystems/flake-checker-action@main
        with:
          check-owner: false
          check-supported: false
          fail-mode: true

      - name: Nix Flake Check
        run: nix flake check --show-trace --all-systems

      - name: Build devShell
        run: nix build .#devShell --show-trace

      - name: Build Clan Machines
        run: nix run .#clan-cli -- machines build --debug
      
      - name: Build MkDocs
        run: nix build .#documentation --show-trace

      - name: Setup Pages
        uses: actions/configure-pages@v6.0.0

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5.0.0
        with:
          path: './result'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5.0.0

      - name: Publish to FlakeHub
        uses: DeterminateSystems/flakehub-push@main
        with:
          visibility: public
          name: andrewthomaslee/home
          tag: ${{ inputs.tag }}
          include-output-paths: true
